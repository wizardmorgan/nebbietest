#!/bin/bash
# Prepara mudroot/lib dal mondo di produzione locale + patch ladro.
# I myst.* di produzione non sono nel repository git.
#
# Vedi: ./scripts/prepare-mudlib.sh --help
set -euo pipefail
ROOT="$(cd "$(dirname "$0")" && pwd)"
exec "$ROOT/scripts/prepare-mudlib.sh" "$@"
