# Placeholder

Gli snippet reali non sono ancora stati generati da produzione.

Sulla macchina con `mudroot/lib/` di produzione:

```bash
./scripts/export-production-world-reference.sh
git add world-reference/snippets/
git commit -m "Add production world reference snippets"
git push
```
